## Infrastructure setup plan

No application code under `src/lib/**`, `src/components/**`, or `src/routes/**` will be touched.

### 1. GitHub sync

Lovable's GitHub integration is bidirectional and auto-syncs — nothing for me to run. I'll just verify the working tree reflects the latest commit before proceeding.

### 2. Missing migration file

Only `0001_init.sql` and `0002_seed.sql` are present in `supabase/migrations/`. **`0003_presets_v2.sql` does not exist in the repo.** I need you to either:

- push it to GitHub so it syncs in, or
- paste the SQL here so I can apply it.

I will NOT invent its contents.

### 3. Apply migrations

Apply via the Supabase migration tool, in order:

1. `0001_init.sql` — schema (sessions, messages, assets, presets, model_capabilities), RLS, triggers
2. `0002_seed.sql` — seed data
3. `0003_presets_v2.sql` — once provided

Each runs through the standard approval flow.

### 4. Storage bucket

`0001_init.sql` likely creates the `studio-images` bucket via SQL. Per Lovable conventions, bucket creation must go through `supabase--storage_create_bucket` (private), with RLS policies on `storage.objects` applied via migration. I'll create the bucket with that tool and run only the policy SQL from the migration, skipping any `insert into storage.buckets` line.

### 5. Regenerate types

`src/integrations/supabase/types.ts` is auto-generated by the Supabase integration after migrations apply — it'll refresh automatically. I'll verify it contains the new tables afterward.

### 6. Server secrets

Add via the secrets tool (you'll enter values in the secure form):

- `GEMINI_API_KEY`
- `REPLICATE_API_TOKEN`
- `OPENAI_API_KEY`

Note: `LOVABLE_API_KEY` is already provisioned and could replace `GEMINI_API_KEY`/`OPENAI_API_KEY` via the Lovable AI Gateway if you want — but since the app code (`gemini.server.ts`, etc.) reads these specific names, I'll add them as requested.

### 7. Google auth

Run `supabase--configure_social_auth` with `providers: ["google"]`. Managed Google OAuth is enabled by default — no client ID/secret needed. The `@frankbody.com` domain restriction is enforced in app code (`auth.functions.ts`), not in Supabase config, so nothing to change there.

### 8. Verification

End-to-end QA (sign in → create session → generate with Nano Banana Pro → refresh) requires a real `@frankbody.com` Google account and only you can perform it. I'll confirm the infra side: migrations applied, bucket exists, secrets present, Google provider on.

---

**Blocker:** I need `0003_presets_v2.sql` contents (or for it to sync from GitHub) before I can proceed past step 3.2. Want me to start with steps 1–2 and the secrets/auth steps in parallel while you provide it?
